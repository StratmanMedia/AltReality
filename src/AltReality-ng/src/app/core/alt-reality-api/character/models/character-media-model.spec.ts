import { CharacterMediaModel } from './character-media-model';

describe('CharacterMediaModel', () => {
  it('should create an instance', () => {
    expect(new CharacterMediaModel()).toBeTruthy();
  });
});
